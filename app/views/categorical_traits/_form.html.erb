<%= error_messages_for(@categorical_trait) %>
  <table>
    <tr>
      <th><%= f.label(:name, "Categorical Trait Name") %></th>
      <td><%= f.text_field(:name) %></td>
    </tr>
    <tr>
      <th><%= f.label(:display_format_id, "Display Format") %></th>
      <td><%= f.select(:display_format_id, options_from_collection_for_select(DisplayFormat.all, 'id','name', @categorical_trait.display_format_id)) %></td>
    </tr>
  </table>