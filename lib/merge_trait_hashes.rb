class Array
  # working on continuous for now
  def merge_trait_hashes(method=:avg)
    # members of the array will be hashes with
    # {trait_id => {:values => [], :sources => {}, :notes => ??, :value_matches => {}}}
    # Given an array of trait hashes
    return first
  end
end
